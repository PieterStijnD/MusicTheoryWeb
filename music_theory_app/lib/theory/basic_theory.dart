import 'package:flutter/material.dart';

class BasicTheoryPage extends StatelessWidget {
  const BasicTheoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Basic Theory Page"),
      ),
    );
  }
}